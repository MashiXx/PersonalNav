<!DOCTYPE html>
<html lang="<%= locale %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Personal NAV</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body>
    <%- include('../partials/header') %>

    <main class="container">
        <%- include('../partials/flash') %>

        <div class="page-header">
            <h1 class="page-title"><%= t('assetGroups.title') %></h1>
            <a href="/asset-groups/create" class="btn btn-primary"><%= t('assetGroups.addNew') %></a>
        </div>

        <% if (groups.length === 0) { %>
            <p class="empty-state"><%= t('assetGroups.emptyState') %> <a href="/asset-groups/create"><%= t('common.createNow') %></a></p>
        <% } else { %>
            <table class="data-table">
                <thead>
                    <tr>
                        <th><%= t('common.icon') %></th>
                        <th><%= t('assetGroups.groupName') %></th>
                        <th><%= t('common.type') %></th>
                        <th><%= t('common.currency') %></th>
                        <th><%= t('assetGroups.assetCount') %></th>
                        <th><%= t('common.totalValue') %></th>
                        <th><%= t('common.actions') %></th>
                    </tr>
                </thead>
                <tbody>
                    <% groups.forEach(group => { %>
                        <tr>
                            <td><img src="/images/icons/<%= group.icon || 'other.svg' %>" alt="" class="group-icon"></td>
                            <td><%= group.name %></td>
                            <td><%= group.type %></td>
                            <td><%= group.currency %></td>
                            <td><%= group.assetCount %></td>
                            <td><%= formatCurrency(group.totalValue) %></td>
                            <td>
                                <a href="/asset-groups/<%= group.id %>" class="btn btn-sm btn-primary"><%= t('common.view') %></a>
                                <a href="/asset-groups/<%= group.id %>/edit" class="btn btn-sm"><%= t('common.edit') %></a>
                                <% if (group.assetCount > 0) { %>
                                    <button type="button" class="btn btn-sm btn-danger" disabled title="<%= t('assetGroups.cannotDeleteWithData') %>"><%= t('common.delete') %></button>
                                <% } else { %>
                                    <form action="/asset-groups/<%= group.id %>/delete" method="POST" style="display: inline;">
                                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('<%= t('common.confirmDelete') %>')"><%= t('common.delete') %></button>
                                    </form>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
