<!DOCTYPE html>
<html lang="<%= typeof locale !== 'undefined' ? locale : 'vi' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Personal NAV</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body>
    <% if (user && user.userId) { %>
        <%- include('partials/header') %>
    <% } %>

    <main class="container">
        <div class="error-page">
            <h1><%= title %></h1>
            <p><%= message %></p>
            <a href="<%= user && user.userId ? '/dashboard' : '/auth/login' %>" class="btn btn-primary"><%= typeof t !== 'undefined' ? t('common.back') : 'Quay lại' %></a>
        </div>
    </main>

    <% if (user && user.userId) { %>
        <%- include('partials/footer') %>
    <% } %>
</body>
</html>
