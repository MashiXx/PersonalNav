<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Personal NAV</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body>
    <%- include('../partials/header') %>

    <main class="container">
        <%- include('../partials/flash') %>

        <h1 class="page-title">Thêm khoản nợ mới</h1>

        <form action="/debts" method="POST" class="form">
            <div class="form-group">
                <label for="name">Tên khoản nợ *</label>
                <input type="text" id="name" name="name" required class="form-control">
            </div>

            <div class="form-group">
                <label for="description">Mô tả</label>
                <textarea id="description" name="description" rows="4" class="form-control"></textarea>
            </div>

            <div class="form-group">
                <label for="amount">Số tiền *</label>
                <input type="number" id="amount" name="amount" step="0.01" required class="form-control">
            </div>

            <div class="form-group">
                <label for="interestRate">Lãi suất (%/năm)</label>
                <input type="number" id="interestRate" name="interestRate" step="0.01" value="0" class="form-control">
            </div>

            <div class="form-group">
                <label for="currency">Đơn vị tiền tệ *</label>
                <select id="currency" name="currency" required class="form-control">
                    <% currencies.forEach(function(c) { %>
                        <option value="<%= c.code %>"><%= c.code %> (<%= c.symbol %>)</option>
                    <% }) %>
                </select>
            </div>

            <div class="form-group">
                <label for="dueDate">Hạn thanh toán</label>
                <input type="date" id="dueDate" name="dueDate" class="form-control">
            </div>

            <div class="form-group">
                <label for="status">Trạng thái *</label>
                <select id="status" name="status" required class="form-control">
                    <option value="active">Đang hoạt động</option>
                    <option value="paid_off">Đã thanh toán</option>
                    <option value="overdue">Quá hạn</option>
                </select>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Thêm khoản nợ</button>
                <a href="/debts" class="btn btn-secondary">Hủy</a>
            </div>
        </form>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
